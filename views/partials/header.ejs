<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <header>
        <h1>JTE Collective</h1>
        <nav>
            <a href="/listings"><button <%- title === 'All Listings' ? 'class="active"' : '' %>>Browse</button></a>
            <a href="/categories"><button <%- title === 'All Categories' ? 'class="active"' : '' %>>Categories</button></a>

            <% if (user) { %>
                <a href="/listings/new"><button <button <%- title === 'New Listing' ? 'class="active"' : '' %>>New Listing</button></a>
                <a href="/categories/new"><button <button <%- title === 'New Category' ? 'class="active"' : '' %>>New Category</button></a>
                <a href="/logout"><button>Log Out</button></a>
            <% } else { %>
                <a href="/auth/google"><button class="button">Login</button></a>
            <% } %>
        </nav>
      </header>
      <main>